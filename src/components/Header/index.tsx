import NavHeader from './NavHeader';
import UpperHeader from './UpperHeader';

const Header = () => {
  return (
    <section>
      <UpperHeader />
      <NavHeader />
    </section>
  );
};

export default Header;
